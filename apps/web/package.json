{
  "name": "@denga/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "next build",
    "dev": "next dev",
    "lint": "eslint . --max-warnings=0",
    "test": "jest --config jest.config.ts --runInBand"
  },
  "devDependencies": {
    "react-test-renderer": "^19.2.5"
  }
}
